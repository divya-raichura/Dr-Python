# assert - This keyword is used as the debugging tool in Python.
# It checks the correctness of the code. It raises an AssertionError
# if found any error in the code and also prints the message with an error.

# so syntax meaning : assert 'condition', message
#  if the condition is false code returns assertion error, and
#  you can add a message(not compulsory)

# in following, condition is true b = 1, so no error
# a = 10
# b = 1
# assert b == 1, 'galat bhai'
# print(a / b)


# a = 10
# b = 0
# assert b == 0, 'zero se divide karra hai tu'
# print(a / b)


# a = 10
# b = 0
# assert b != 0, 'zero se divide karra hai tu'
# print(a / b)
